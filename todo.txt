* Make the client's wallet files to work with multisig json address description files
* Make the dowloader to figure out a proper IP of a hub node / work with gocoion.cfg file
* Fix downloader getting stuck at "Last Header Height: 0"
* Improve the database folder locking in Linux
* Try to make own (faster) implementation of sha256 and rimp160
* The best chain should be decided on the amount of hashing work, not the length - SetBestChain()
